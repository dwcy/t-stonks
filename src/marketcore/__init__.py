"""marketcore — reusable lower layer for terminal market dashboards.

Symbol-agnostic foundation (models, HTTP, session, paths, polling services,
widgets) shared by app packages such as ``goldsilver`` and ``quantum``. This
package must never import an app package.
"""
